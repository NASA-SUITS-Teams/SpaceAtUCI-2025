{
  "name": "backend-suits-uci",
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n hono,fast -c blue,green \"cd backend && npm run dev\" \"cd python && uvicorn index:app --reload\"",
    "postinstall": "cd backend && npm install && cd ../python && pip install -r requirements.txt",
    "format": "prettier --write **/*.{css,scss,cjs,js,ts,tsx,yml,json,html}",
    "lint": "prettier **/*.{css,scss,cjs,js,ts,tsx,yml,json,html} --check && cd site && npm run lint && cd ../api && npm run lint"
  },
  "dependencies": {
    "bun": "^1.2.0",
    "dgram": "^1.0.1",
    "dotenv-flow": "^4.0.1",
    "hono": "^4.6.17"
  },
  "engines": {
    "node": "^18 || ^20"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.2",
    "@typescript-eslint/parser": "^7.0.2",
    "concurrently": "^8.2.2",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
